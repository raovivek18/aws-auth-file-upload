type FileMetadata @model @auth(rules: [
  { allow: owner },
  { allow: public, operations: [read] }
]) {
  id: ID!
  name: String!
  size: Int!
  type: String!
  key: String!
  owner: String! @index(name: "byOwner", queryField: "listFileMetadataByOwner", sortKeyFields: ["uploadTimestamp"])
  sharingStatus: SharingStatus
  shareExpiration: AWSDateTime
  uploadTimestamp: AWSDateTime!
}

type ActivityLog @model @auth(rules: [
  { allow: owner }
]) {
  id: ID!
  userId: String! @index(name: "byUser", queryField: "listLogsByUser", sortKeyFields: ["timestamp"])
  actionType: String!
  timestamp: AWSDateTime!
  fileId: String
  fileName: String
  ip: String
}

enum SharingStatus {
  PRIVATE
  PUBLIC
}

type UserAnalytics @model @auth(rules: [
  { allow: owner }
]) {
  id: ID! # userId (sub)
  totalFiles: Int!
  totalStorage: AWSInt! # Total bytes used
  totalShares: Int!
  storageLimit: AWSInt! # Optional: for quota tracking
  lastActive: AWSDateTime!
}

type SharedFile @model @auth(rules: [
  { allow: owner }, # The sharer is the creator/owner
  { allow: owner, ownerField: "sharedWith", operations: [read] } # The recipient has read access
]) {
  id: ID!
  fileId: ID! @index(name: "byFile")
  sharedWith: String! @index(name: "byRecipient", queryField: "listSharedWithMe", sortKeyFields: ["createdAt"]) # recipient email
  ownerId: String!
  ownerEmail: String
  fileName: String!
  fileKey: String!
  fileSize: Int
  fileType: String
  createdAt: AWSDateTime!
}
